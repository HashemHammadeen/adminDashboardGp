<div class="max-w-4xl mx-auto">
  <div class="mb-6 flex items-center justify-between">
    <div>
      <h1 class="text-2xl font-bold text-gray-900">Add New User</h1>
      <p class="text-sm text-gray-600">Register a new customer for the loyalty program.</p>
    </div>
    <%= link_to 'Back to List', users_path, class: "text-sm font-medium text-indigo-600 hover:text-indigo-500" %>
  </div>

  <div class="bg-white shadow sm:rounded-lg border border-gray-200">
    <div class="px-4 py-5 sm:p-6">
      <%# We pass the @user object to the partial %>
      <%= render "form", user: @user %>
    </div>
  </div>

  <div class="mt-6 flex items-start space-x-3 text-sm text-gray-500 italic">
    <span class="text-indigo-500">💡</span>
    <p>Creating a new user will automatically initialize a points balance of 0 and a lifetime points record.</p>
  </div>
</div>